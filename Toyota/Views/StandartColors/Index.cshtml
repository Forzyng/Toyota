@model IEnumerable<Toyota.Data.Entities.Color>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
    <div class="col-sm-10">
        <button type="button" class="btn btn-primary" id="BackupSend" value="Backup">Backup</button>
    </div>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Slug)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImgUrl)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Slug)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <img class="admin_model_thumb" src="@Html.DisplayFor(modelItem => item.ImgUrl)"/>
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                
            </td>
        </tr>
}
    </tbody>
</table>

<script>
    let btn = document.getElementById("BackupSend");

    btn.onclick = function () {
        //var b = new Helper.Database.Dump.Create();
        //    b.DumpColors(_context);
        // console.log("Backuped");
    }
</script>
